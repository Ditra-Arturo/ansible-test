---
- name: Install Apache on mi_servidor
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Install Apache2 Package
      ansible.builtin.apt:
        name: apache2
        #state: present
      when: ansible_distribution == "Fedora"
